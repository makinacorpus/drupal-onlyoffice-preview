name: 'Onlyoffice Preview Field Module'
description: 'Field to display a document in iframe via an Onlyoffice server'
package: 'Makina'

type: module
core_version_requirement: ^8 || ^9

dependencies:
  - drupal:media_library

configure: onlyoffice_preview.settings